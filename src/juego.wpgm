import wollok.game.*
import ralph.*
import animacion.*
import ventana.*
import juego.*

program fixItFelix {
	game.title("Fix It Felix Jr!")
	
	game.width(100)
	game.height(60)
	game.cellSize(10)
	
	keyboard.space().onPressDo({ juego.siguienteNivel() })
 	
 	juego.preparar()
	juego.iniciar()
	
}
