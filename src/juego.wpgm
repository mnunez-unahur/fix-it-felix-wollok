import wollok.game.*
import ralph.*

program juego {
	game.title("Juego Base")
	
	game.width(100)
	game.height(60)
	game.cellSize(10)	
  	game.boardGround("fondo/nivel-1.png")
  	
	game.addVisual(ralph)
	game.start()
}